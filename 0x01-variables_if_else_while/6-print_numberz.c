#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int numberz = 48;

	while (numberz <= 57)
	{
		putchar(numberz);
		numberz++;
	}
	putchar(10);
	return (0);
}
