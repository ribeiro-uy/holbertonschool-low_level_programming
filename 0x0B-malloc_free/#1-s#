char *array;
        unsigned int i;

        array = malloc((size + 1) * sizeof(char));

        if (array == NULL)
                return (NULL);

        i = 0;

        while (i < size)
        {
                array[i] = c;
                i++;
        }

        array[i] = '\0';
        return (array);