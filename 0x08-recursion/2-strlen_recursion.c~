#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */

int _strlen_recursion(char *s)
{
	int len = 0;

	if (*s == '\0')
		return(0);
	else
	{
		_strlen_recursion(s + 1);
		len++;
	}

	return(len);
}
